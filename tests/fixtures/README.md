# OCR reference images

These two PNGs were rendered from the synthetic greeting “こんにちは世界” in the Yomu browser test on Windows. They contain no third-party comic artwork. Horizontal and vertical images are fixed inputs so Linux and Windows test the same raster rather than different platform fonts. The real bundled OCR models and the existing text/orientation assertions remain enabled.
